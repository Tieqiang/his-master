package ${packageName}.domain.${moduleName}.facade;

import com.jims.his.common.BaseFacade;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;


/**
* ${functionName}
* @author ${classAuthor}
* @version ${classVersion}
*
*/
public class ${ClassName}Facade extends BaseFacade {
    private final Logger LOGGER = LoggerFactory.getLogger(${ClassName}Facade.class);

}
