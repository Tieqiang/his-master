/**
 * There are <a href="https://github.com/thinkgem/jeesite">JeeSite</a> code generation
 */
package ${packageName}.${moduleName}.facade${subModuleName};

import org.springframework.stereotype.Repository;

import com.jims.his.common.persistence.BaseDao;
import com.jims.his.common.persistence.Parameter;
import ${packageName}.${moduleName}.entity${subModuleName}.${ClassName};

/**
 * ${functionName}DAO接口
 * @author ${classAuthor}
 * @version ${classVersion}
 */
@Repository
public class ${ClassName}Facade extends BaseFacade {
	
}
